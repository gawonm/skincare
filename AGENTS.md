# 코드 작성 규칙

## 1. 클래스 기반 작성

모든 코드는 클래스 기반으로 작성한다.

- 기능 단위는 모듈 레벨 함수가 아니라 클래스의 메서드로 구현한다.
- 상태와 동작은 하나의 클래스 안에 묶는다.
- 전역 변수, 전역 함수로 로직을 흩뿌리지 않는다.

## 2. 타입 정의는 enum / Pydantic 기반

함수·메서드의 파라미터와 반환값에 사용하는 타입은 `enum` 또는 Pydantic 모델로 정의한다.

- 고정된 값의 집합은 `Enum`(또는 `StrEnum`)으로 정의하고, 문자열 리터럴을 직접 쓰지 않는다.
- 구조화된 데이터는 Pydantic 모델로 정의하고, `dict`나 튜플을 그대로 주고받지 않는다.
- 컬렉션은 Pydantic 모델을 원소로 하는 제네릭 형태로 명시한다.
  - `list[SomeModel]`
  - `dict[str, SomeModel]`
  - `dict[SomeEnum, list[SomeModel]]`
- 파라미터와 반환값 모두에 타입 힌트를 빠짐없이 붙인다.

## 3. 불확실한 내용은 임의로 결정하지 않는다

작업 도중 확실하지 않은 부분이 나오면 임의로 판단해서 진행하지 말고, 반드시 사용자에게 질문하여 선택을 받는다.

질문할 때는 다음을 쉽고 자세하게 함께 설명한다.

- 왜 이 선택이 필요한지 (어떤 부분이 불명확한지)
- 각 선택지가 어떤 결과를 가져오는지 (장단점, 영향 범위)
- 추천하는 선택지가 있다면 그 이유

사용자의 답변을 받기 전까지 해당 부분은 진행하지 않는다.

## 4. 주석은 한국어로, "왜"를 적는다

코드만 봐도 아는 내용은 주석으로 쓰지 않는다. 왜 이렇게 했는지를 적는다.

```python
# 나쁨: 무엇을 하는지만 적음
# 리스트를 순회한다
for item in items:
    ...

# 좋음: 이유를 적음
# 순서가 뒤섞이면 마이그레이션이 깨지므로 정렬된 상태로만 순회한다
for item in sorted(items):
    ...
```

## 5. 코드를 바꾼 뒤에는 설명한다

파일을 수정하면 세 줄 정도로 요약해서 알려준다.

- 무엇을 바꿨는지
- 왜 바꿨는지
- 사용자가 확인해야 할 것이 있는지

## 6. 새 라이브러리는 먼저 물어본다

의존성을 추가하기 전에 아래를 설명하고 허락을 받는다.

- 왜 필요한지
- 없이도 되는지, 안 된다면 그 이유

## 7. 에러를 숨기지 않는다

- `except: pass`, 내용이 빈 `try` 블록은 쓰지 않는다.
- 예외를 잡을 때는 잡을 예외 종류를 명시하고, 무엇이 잘못됐는지 알 수 있는 메시지를 남긴다.

```python
# 나쁨: 실패해도 아무도 모른다
try:
    connect_db()
except:
    pass

# 좋음: 무엇이 왜 실패했는지 드러난다
try:
    connect_db()
except ConnectionError as e:
    raise RuntimeError(f"DB 연결 실패: {e}") from e
```

## 8. 요청한 것만 한다

- 요청 범위 밖의 리팩터링, 파일 정리, 이름 변경은 하지 않는다.
- 고칠 만한 부분을 발견하면 직접 고치지 말고 먼저 알려준다.

## 9. 매직 넘버와 매직 문자열을 쓰지 않는다

의미 있는 값은 이름을 붙여서 상수나 `Enum`으로 뺀다. (규칙 2와 같은 맥락)

```python
# 나쁨: 30이 무슨 뜻인지 알 수 없다
if retry_count > 30:
    ...
if status == "done":
    ...

# 좋음
MAX_RETRY_COUNT = 30

class TaskStatus(StrEnum):
    DONE = "done"

if retry_count > MAX_RETRY_COUNT:
    ...
if status is TaskStatus.DONE:
    ...
```

## 10. 정해진 폴더 구조를 지킨다

전체 구조와 각 폴더의 역할은 `STRUCTURE.md`에 있다. 새 파일은 그 구조 안에 만든다.

| 폴더 | 넣는 것 | 넣지 않는 것 |
| --- | --- | --- |
| `core/` | 설정, DB 연결, Redis, 공용 타입 | 업무 로직 |
| `models/` | SQLAlchemy 테이블 정의 | 업무 로직, API 응답용 모델 |
| `agent/rag/` | 문서 로딩·자르기·임베딩·검색·답변 생성 | FastAPI 코드, HTTP 관련 코드 |
| `agent/tools/` | 에이전트가 호출하는 도구 | FastAPI 코드, HTTP 관련 코드 |
| `backend/api/` | 엔드포인트 | 업무 로직, DB 쿼리 |
| `backend/schemas/` | 요청/응답 Pydantic 모델 | 테이블 정의 |
| `backend/services/` | 업무 로직, agent 호출, 트랜잭션 | SQLAlchemy 쿼리 |
| `backend/repositories/` | DB 조회/저장 쿼리 | 업무 로직, commit |
| `frontend/` | 화면 코드 | 파이썬 코드 |

새 폴더가 필요해 보이면 만들기 전에 먼저 물어본다.

## 11. import 방향을 지킨다

```
frontend  →  backend  →  agent  →  core
                 └──────────────→  models
```

backend 안에서도 순서가 있다.

```
api  →  services  →  repositories  →  models
            └─────→  agent
```

agent 안에서도 순서가 있다.

```
rag  →  core
tools  →  core
```

화살표 방향으로만 import 한다. 반대 방향으로 import 하면 두 폴더가 서로를 필요로 하게 되어,
한쪽만 떼어내 테스트하거나 수정할 수 없게 된다.

- `core`는 아무것도 import 하지 않는다.
- `agent`는 `backend`를 import 하지 않는다. FastAPI를 몰라야 한다.
- `agent`는 DB 세션을 직접 만들지 않는다. 필요하면 파라미터로 받는다.

## 12. DB 접근은 리포지토리에만 쓴다

SQLAlchemy 쿼리(`select`, `insert` 등)는 `backend/repositories/` 안에서만 작성한다.
엔드포인트나 서비스에 쿼리를 직접 쓰지 않는다.

- 테이블 하나당 리포지토리 클래스 하나로 만든다.
- 세션은 생성자에서 주입받는다. 리포지토리가 직접 만들지 않는다.
- 반환은 SQLAlchemy 모델 또는 Pydantic 모델로 한다. dict, 튜플로 내보내지 않는다.
- `commit`은 리포지토리에서 하지 않는다. 언제 확정할지는 `services/`가 정한다.

이렇게 나누면 같은 조회가 여러 곳에 다르게 복사되는 일이 없고, 쿼리를 고칠 때 한 파일만 보면 된다.

## 13. 모델을 추가하면 설정도 같이 고친다

`models/`에 새 파일을 만들면 `config.yaml`의 `database.model_modules`에 모듈 경로를 반드시 추가한다.
빠뜨리면 Alembic이 모델을 인식하지 못해 마이그레이션에 아무 변경도 나오지 않는다.
원인을 찾기 어려운 실수라 항상 같이 확인한다.

## 14. DB를 만들기 전에 ERD 문서를 먼저 공유한다

테이블을 새로 만들거나 기존 테이블의 구조를 바꾸기 전에, 먼저 ERD 문서를 작성해서 사용자에게
공유하고 확인을 받는다. 문서 없이 `models/`에 파일을 만들거나 마이그레이션을 생성하지 않는다.

- 경로는 `docs/erd/<db이름>.md` 로 한다. DB 하나당 파일 하나다.
- 문서에는 다음을 넣는다.
  - Mermaid `erDiagram` 으로 그린 전체 관계도
  - 테이블별 컬럼 표 (컬럼명, 타입, NULL 허용 여부, 기본값, 설명)
  - 기본키·외래키·유니크 제약·인덱스
  - 왜 이렇게 나눴는지 (정규화 판단, 비정규화한 곳이 있다면 그 이유)
- 순서를 지킨다: ERD 문서 작성 → 사용자 확인 → `models/` 작성 → 마이그레이션 생성.
- 이미 있는 테이블을 바꿀 때도 ERD 문서를 먼저 고치고 확인을 받는다.
- 스키마를 바꾼 뒤에는 ERD 문서도 같이 고쳐서 실제 DB와 문서가 어긋나지 않게 한다.

먼저 그림으로 합의하지 않으면 마이그레이션을 만든 뒤에야 설계가 틀린 것을 알게 되고,
그때는 되돌리는 비용이 훨씬 크다.

## 15. 자기 파트 문서만 읽고, 자기 파트만 구현한다

이 프로젝트는 여러 명이 파트를 나눠 작업한다. 작업을 시작하기 전에 **반드시** 해당 파트의
문서를 먼저 읽고, 그 문서에 적힌 담당 범위 안에서만 구현한다.

| 파트 | 진입 문서 |
| --- | --- |
| agent | `docs/agent/README.md` |
| backend | `docs/backend/README.md` |
| front | `docs/front/README.md` |
| data | `docs/data/README.md` |

### 어느 파트인지 판정하는 법

요청 문구가 아니라 **손대게 될 파일 경로**로 판정한다. 문구는 사람마다 다르게 쓰지만 경로는
하나로 정해져 있다.

| 손대는 경로 | 파트 | 먼저 읽을 문서 |
| --- | --- | --- |
| `agent/` | agent | `docs/agent/README.md` |
| `backend/` | backend | `docs/backend/README.md` |
| `frontend/` | front | `docs/front/README.md` |
| `models/`, `migrations/`, `data/scripts/` | data | `docs/data/README.md` |
| `core/` | 공용 | 아래 "공용 코드" 참고 |
| `config.yaml`, `docker-compose.yml`, `justfile` 등 루트 설정 | 공용 | 아래 "공용 코드" 참고 |

**공용 코드**: `core/` 와 루트 설정 파일은 어느 한 파트의 소유가 아니다. 여기를 고쳐야 하면
바로 고치지 말고, 어느 파트 작업 중에 왜 필요해졌는지 설명하고 사용자에게 먼저 묻는다.
모든 파트가 영향을 받기 때문이다.

### 여러 파트에 걸치는 요청이면 멈추고 되묻는다

요청 하나가 위 표의 경로 **두 개 이상**을 건드리게 되면 (예: "데이터 수집해서 변환하고 백엔드
API 로 내려줘" → `data/scripts/` + `backend/`), 그대로 진행하지 않는다. 코드를 쓰기 전에 멈추고
사용자에게 다음을 묻는다.

1. **지금 하려는 게 어느 파트의 개발인가?**
   한 파트만 하고 나머지는 다른 담당자 몫인지 확인한다. 그렇다면 그 파트만 구현하고,
   나머지는 손대지 않는다.
2. **아니면 파트 경계에 해당하는 작업인가?**
   두 파트가 값을 주고받는 지점이라 양쪽을 함께 봐야 하는 경우다. 그렇다면 규칙 16을 따른다.
   계약 문서를 먼저 쓰고 상대 담당자와 합의한 뒤에 구현한다. 합의 전에는 상대 파트 폴더에
   파일을 만들지 않는다.

답을 받기 전까지는 어느 쪽도 구현하지 않는다 (규칙 3). 물을 때는 어떤 경로들이 걸려서 묻는지,
각각 어느 파트인지 같이 밝힌다.

```
요청: "성분 데이터 수집해서 추천 API 로 내려줘"

걸리는 경로: data/scripts/ (data 파트), backend/api/ + backend/services/ (backend 파트)

질문: 두 파트에 걸칩니다. 둘 중 무엇인가요?
  (1) 지금은 data 파트만 하고 API 는 backend 담당자가 따로 하나요?
  (2) 두 파트가 값을 주고받는 경계 작업인가요?
      그렇다면 docs/contracts/data-to-backend.md 에 넘길 값의 형태를 먼저 쓰고
      합의한 뒤에 구현하겠습니다 (규칙 16).
```

지켜야 할 것.

- 작업 시작 전에 자기 파트의 `README.md` 를 먼저 읽는다. 읽지 않은 상태로 코드를 쓰지 않는다.
- `README.md` 가 같은 폴더의 다른 md 를 가리키면 그것도 읽는다 (예: `docs/data/data.md`).
- 각 문서의 "담당 범위" 절에 적힌 범위 밖은 구현하지 않는다. 범위 밖으로 보이면 멈추고 사용자에게 묻는다.
- 어느 파트인지 불명확하면 임의로 고르지 말고 사용자에게 묻는다 (규칙 3).
- 다른 파트의 산출물이 필요하면 직접 만들지 말고, 인터페이스(스키마·함수 시그니처)만 정하고 사용자에게 알린다.
- 파트 문서와 이 문서(CLAUDE.md)가 충돌하면 이 문서를 따르고, 충돌을 사용자에게 알린다.

한 번에 처음부터 끝까지 다 만들어 버리면 다른 사람이 맡은 부분을 덮어쓰게 되고,
합칠 때 충돌이 나서 서로의 작업이 사라진다. 그래서 범위를 문서로 못 박아 둔다.

## 16. 파트 경계는 먼저 문서로 합의하고 넘긴다

한 파트가 다른 파트의 코드를 부르거나, 같은 테이블·같은 데이터를 함께 쓰는 지점을 **경계**라고
한다. 경계에 해당하는 코드는 혼자 정하고 만들지 않는다. 먼저 계약 문서를 쓰고, 상대 담당자에게
설명하고, 합의한 뒤에 구현한다.

### 언제 이 규칙을 적용하나

- 다른 파트의 클래스·함수를 호출할 때 (예: `backend/services/` 에서 `agent` 의 RAG 파이프라인 호출)
- 다른 파트가 쓸 응답 형태를 정할 때 (예: backend API 응답 → front 화면)
- 다른 파트가 만든 테이블을 읽거나, 두 파트가 같은 테이블에 쓸 때
- 다른 파트가 만든 산출물 파일(CSV/Parquet 등)의 컬럼을 정할 때
- 이미 합의된 시그니처·응답 형태·컬럼을 **바꿀 때** (새로 만들 때보다 이쪽이 더 중요하다)

### 계약 문서 경로

```
docs/contracts/<보내는파트>-to-<받는파트>.md
```

예: `docs/contracts/backend-to-agent.md`, `docs/contracts/front-to-backend.md`,
`docs/contracts/data-to-agent.md`.

호출하는 쪽(값을 넘기는 쪽)이 초안을 쓴다. 자기가 무엇을 갖고 있고 무엇이 필요한지 아는 쪽이
호출하는 쪽이기 때문이다.

### 계약 문서에 적을 것

- **부르는 대상**: 클래스명과 메서드명 (또는 HTTP 메서드와 경로)
- **입력**: 파라미터 이름, 타입, 필수/선택, 예시 값
  - 타입은 Pydantic 모델과 Enum 으로 적는다. dict 나 튜플로 적지 않는다 (규칙 2).
  - 모델 정의 코드를 그대로 붙여 넣는다. 말로만 설명하지 않는다.
- **출력**: 반환 타입과 필드 설명, 예시 값
- **누가 이 타입을 소유하나**: 어느 폴더의 어느 파일에 정의하는지
  - import 방향상 **불리는 쪽**이 자기 입출력 모델을 소유한다 (규칙 11).
    `agent` 를 부르면 모델은 `agent/rag/schemas.py` 에, `backend` API 를 부르면
    `backend/schemas/` 에 있다. 부르는 쪽이 자기 폴더에 복사본을 만들지 않는다.
- **실패했을 때**: 어떤 예외를 던지는지, 어떤 에러 응답을 주는지 (규칙 7)
- **아직 안 정한 것**: 비워 두고 "미정" 이라고 적는다. 임의로 채우지 않는다 (규칙 3).

### 절차

1. 호출하는 쪽이 `docs/contracts/` 에 초안을 쓴다.
2. 사용자에게 알린다. 무엇을 왜 이렇게 넘기려는지, 상대 파트에 무엇을 요청하는지 설명한다.
3. 상대 파트 담당자와 논의해 확정한다.
4. 확정된 뒤에 구현한다. 불리는 쪽이 먼저 타입과 빈 껍데기를 만들고, 부르는 쪽이 그걸 쓴다.
5. 양쪽 파트의 `README.md` "관련 문서" 에 계약 문서 링크를 건다.

**합의 전에는 상대 파트 폴더에 파일을 만들지 않는다.** 필요한 클래스가 아직 없으면 직접 만들지
말고, 계약 문서에 "이 시그니처가 필요하다" 고 적고 멈춘다.

### 계약을 바꿀 때

- 계약 문서를 먼저 고치고, 무엇이 어떻게 바뀌는지와 상대 코드에 어떤 영향이 가는지 설명한다.
- 합의 없이 시그니처나 응답 필드를 바꾸지 않는다. 상대 코드가 조용히 깨진다.
- 필드를 지우거나 이름을 바꾸는 변경은 특히 먼저 알린다. 추가는 비교적 안전하다.

경계에서 생기는 사고는 대부분 "각자 자기 머릿속 형태로 만들어 놓고 나중에 합쳤더니 안 맞더라"
에서 온다. 미리 문서로 맞춰 두면 합칠 때 고칠 게 없다.

## 17. 파일 업로드·다운로드는 정해진 방식만 쓴다

제품 이미지, 사용자가 올리는 사진 같은 파일은 아래 방식으로만 다룬다. 사람마다 다르게 만들면
배포할 때 한쪽은 파일이 사라지고 한쪽은 안 사라지는 식으로 갈라진다.

### 저장 위치

로컬 볼륨에 저장한다. 경로는 저장소 루트의 `uploads/` 이고, 컨테이너에서는 `/app/uploads` 다.

- **DB에는 경로만 넣는다.** 파일 내용(bytes)을 컬럼에 넣지 않는다. DB 백업이 무거워지고
  마이그레이션이 느려진다.
- `uploads/` 는 `.gitignore` 대상이다. 커밋하지 않는다.
- 컨테이너는 재생성되면 안의 파일이 사라진다. 그래서 반드시 볼륨으로 마운트해야 한다.
  compose 에 볼륨을 추가하는 것은 공용 설정 변경이므로 먼저 사용자에게 묻는다 (규칙 15).
- 나중에 백엔드를 여러 대로 늘리면 이 방식은 대수만큼 파일이 갈라진다. 그때는 오브젝트
  스토리지로 옮겨야 하고, 옮기는 시점은 사용자가 정한다. 미리 S3 코드를 넣지 않는다.

### 파일 이름

원본 파일명을 그대로 저장 경로에 쓰지 않는다.

- 저장할 때는 UUID 등 서버가 만든 이름을 쓴다. 원본 파일명은 DB 컬럼에 따로 보관한다.
- 이유: 사용자가 보낸 이름에 `../` 가 들어오면 의도하지 않은 경로에 쓰게 된다. 한글·공백·
  중복 이름 문제도 함께 사라진다.

### 검증

받는 즉시 검증하고, 통과하지 못하면 저장하지 않는다.

- 허용 확장자와 MIME 타입은 `Enum` 으로 정의한다. 문자열 리터럴을 흩뿌리지 않는다 (규칙 2, 9).
- 최대 크기는 이름 붙인 상수로 둔다 (`MAX_UPLOAD_SIZE_BYTES`).
- 확장자만 믿지 않는다. 실제 내용의 MIME 을 함께 확인한다.
- 검증 실패는 조용히 넘기지 않고 무엇이 왜 거부됐는지 알 수 있는 에러를 낸다 (규칙 7).

### 담당 파트

| 대상 | 파트 |
| --- | --- |
| 업로드·다운로드 API, 저장 경로 관리, 검증 | backend |
| 화면의 파일 선택·미리보기·진행률 | front |
| 수집 스크립트가 내려받는 원본 데이터(엑셀·PDF 등) | data |

수집 스크립트가 받는 원본 파일은 `data/` 아래에 두고 `uploads/` 와 섞지 않는다. 사용자가 올린
것과 우리가 받아온 것은 지우는 기준이 다르기 때문이다.

### 다운로드

- 파일을 내려줄 때는 백엔드가 권한을 확인한 뒤 응답한다. `uploads/` 를 정적 경로로 통째로
  노출하지 않는다. 남의 파일 주소를 추측해 받아 갈 수 있게 된다.
- 파일 경로를 URL 파라미터로 그대로 받지 않는다. DB 에 저장된 식별자로 조회해 경로를 얻는다.

## 18. 배포 구조를 깨지 않는다

배포는 이미 동작하는 형태가 있다. 기능을 추가할 때 아래를 바꾸면 배포가 조용히 깨진다.
바꿔야 할 이유가 생기면 직접 고치지 말고 먼저 사용자에게 알린다.

### 지금의 배포 형태

프론트를 빌드해 백엔드 이미지 안에 넣고, **FastAPI 하나가 API 와 정적 파일을 모두 서빙**한다.
컨테이너는 `postgres`, `redis`, `backend` 셋이다. 자세한 절차는 [README.md](README.md) 참고.

### 바꾸면 안 되는 것

- **단일 오리진.** 프론트를 따로 띄우거나 다른 포트로 노출하지 않는다. 세션 쿠키가
  `SameSite=lax` 이고 백엔드에 CORS 미들웨어가 없어서, 오리진이 갈리면 로그인이 깨진다.
  CORS 미들웨어를 추가해 해결하려 들지 말고 먼저 묻는다.
- **라우터 등록 순서.** `backend/main.py` 는 라우터를 먼저 등록하고 그 뒤에 정적 파일을 `/` 에
  마운트한다. 순서가 바뀌면 정적 파일이 모든 API 경로를 가려 버린다.
- **경로 충돌.** 새 API 는 `/assets` 처럼 빌드 산출물이 쓰는 경로를 피한다.
- **프론트 빌드 산출 경로.** `frontend/dist` 고정이다. Vite 의 `outDir` 을 바꾸면 Dockerfile 의
  복사 경로와 `backend/main.py` 의 마운트 경로가 함께 깨진다.
- **컨테이너 내부 포트 8000.** 호스트 포트만 `.env` 의 `BACKEND_PORT` 로 바꾼다.

### 무언가 추가할 때 같이 해야 하는 것

| 추가한 것 | 같이 해야 할 것 |
| --- | --- |
| 파이썬 패키지 | `uv add` 로 `uv.lock` 까지 갱신. 이미지 빌드는 `--frozen` 이라 lock 이 어긋나면 실패한다 |
| npm 패키지 | `package-lock.json` 커밋. 이미지 빌드는 `npm ci` 라 lock 이 없으면 실패한다 |
| `config.yaml` 설정 키 | `config.yaml.sample` 과 `config.prod.yaml.sample` **둘 다** 갱신 |
| 새 최상위 폴더 (파이썬 패키지) | `Dockerfile` 의 `COPY` 목록에 추가. 빠뜨리면 이미지 안에 그 폴더가 없다 |
| 모델·마이그레이션 | 배포 후 `just prod-migrate` 가 필요하다는 것을 PR 에 적는다 |
| 볼륨에 남아야 하는 데이터 | compose 볼륨 추가. 공용 설정이므로 먼저 묻는다 |

### 설정은 config.yaml 하나뿐이다

`os.environ` 으로 설정을 읽는 코드를 새로 만들지 않는다. 앱의 설정 소스는 `config.yaml` 하나이고
(`core/config.py`), `.env` 는 Compose 전용이다. 두 군데로 갈라지면 어느 값이 적용됐는지
알 수 없게 된다.

컨테이너는 `config.yaml` 을 볼륨으로 받는다. 호스트에 그 파일이 없는 상태로 기동하면 Docker 가
같은 자리에 빈 디렉터리를 만들어 버리고, 앱은 설정을 못 읽어 무한 재시작한다. 배포 전에 파일이
있는지 반드시 확인한다.
