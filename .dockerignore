# 이미지에 들어가면 안 되는 것과, 들어가도 쓸모없어 빌드만 느려지는 것.

.git/
.venv/
__pycache__/
*.pyc

# 호스트에서 만든 node_modules 와 dist 는 이미지 안에서 다시 만든다.
frontend/node_modules/
frontend/dist/

# 비밀값. 이미지에 굽지 않고 실행 시 마운트한다.
config.yaml
.env

# 용량이 크고 런타임에 필요 없다.
data/
docs/
tests/
*.faiss
