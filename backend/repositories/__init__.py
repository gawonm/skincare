"""DB 접근을 담당하는 계층.

한 테이블에 하나의 리포지토리 클래스를 만들고, 그 테이블에 대한 조회와 저장을
전부 여기에 모은다.

이렇게 나누는 이유
- SQLAlchemy 쿼리가 서비스와 엔드포인트 여기저기에 흩어지지 않는다.
- 같은 조회를 두 곳에서 다르게 작성하는 일이 없어진다.
- 서비스를 테스트할 때 리포지토리만 가짜로 바꿔 끼울 수 있다.

규칙
- 세션은 생성자에서 주입받는다. 리포지토리가 직접 만들지 않는다.
- 반환은 SQLAlchemy 모델 또는 Pydantic 모델로 하고, dict 나 튜플로 내보내지 않는다.
- commit 은 여기서 하지 않는다. 트랜잭션 경계는 `services/` 가 정한다.
"""
