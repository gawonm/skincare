# docker-compose.yml 전용. 애플리케이션은 config.yaml 만 읽는다.
# `.env` 로 복사해서 쓰고, `.env` 는 .gitignore 에 넣는다.

POSTGRES_USER=app
POSTGRES_PASSWORD=app
POSTGRES_DB=app
POSTGRES_PORT=5432

REDIS_PORT=6379
REDIS_MAXMEMORY=256mb
